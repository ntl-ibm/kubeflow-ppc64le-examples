<HTML>
<head>
    <meta charset="utf-8" />
    <title>Demo Application Home Page</title>
    <link rel="shortcut icon" href="{{- url_for('static', filename='favicon.ico') -}}" />
    <link rel="stylesheet" href="{{- url_for('static', filename='css/style.css') -}}" />
    <script src="{{- url_for('static', filename='js/handleFormSubmit.js') -}}"></script>
  </head>
<BODY>
 {% include 'nav_bar.jinja' %}

  <div class="slideshow-container">

    <div class="mySlides slideshow-fade">
      <img src="{{- url_for('static', filename='/slideshow/1.jpg') -}}"/>
    </div>

    <div class="mySlides slideshow-fade">
      <img src="{{- url_for('static', filename='/slideshow/2.jpg') -}}"/>
    </div>

    <div class="mySlides slideshow-fade">
      <img src="{{- url_for('static', filename='/slideshow/3.jpg') -}}"/>
    </div>

    <div class="mySlides slideshow-fade">
      <img src="{{- url_for('static', filename='/slideshow/4.jpg') -}}"/>
    </div>

    <div class="mySlides slideshow-fade">
      <img src="{{- url_for('static', filename='/slideshow/5.jpg') -}}"/>
    </div>

    <div class="mySlides sllideshow-fade">
      <img src="{{- url_for('static', filename='/slideshow/6.jpg') -}}"/>
    </div>

    <div class="mySlides slideshow-fade">
      <img src="{{- url_for('static', filename='/slideshow/7.jpg') -}}"/>
    </div>

    <div class="mySlides slideshow-fade">
      <img src="{{- url_for('static', filename='/slideshow/8.jpg') -}}"/>
    </div>

  </div>

  <br>

  <div style="text-align:center">
    <span class="slideshow-dot"></span> 
    <span class="slideshow-dot"></span> 
    <span class="slideshow-dot"></span>
    <span class="slideshow-dot"></span> 
    <span class="slideshow-dot"></span> 
    <span class="slideshow-dot"></span> 
    <span class="slideshow-dot"></span> 
    <span class="slideshow-dot"></span>  
  </div>

  <!-- https://www.w3schools.com/howto/tryit.asp?filename=tryhow_js_slideshow_auto -->
  <script>
    let slideIndex = 0;
    showSlides();

    function showSlide(slide_num, slides, dots) {
      let i
      for (i = 0; i < slides.length; i++)  {
        if (slides[i].classList.includes('mySlides-active')) {
          slides[i].classList.remove('mySlides-active')
        }
      }

      for (i = 0; i < dots.length; i++) {
        if (dots[i].classList.includes('slideshow-dot-active')) {
          dots[i].classList.remove('slideshow-dot-active')
        }
      }

      slides[slide_num].classList.add('mySlides-active')
      if (side_num < dots.length) {
        dots[slide_num].classList.add('mySlides-active')
      }

    }

    function showSlides() {
      let i = 0;
      let slides = document.getElementsByClassName("mySlides");
      let dots = document.getElementsByClassName("slideshow-dot");
      
      while (true) {
        if (i >= slides.length) {
          i = 0
        }
        setTimeout(showSlide, 2000, i, slides, dots)
      }
    }
  </script>
 {% include 'footer.jinja' %}
</BODY>
</HTML>